# Tool capability check

- Checked on (UTC):
- Account/client and connected tool name (no secrets):
- Provider / exact model / mode:
- Evidence: actual tool schema and official documentation URL:
- Accepted image / video / audio reference types and limits:
- How each reference is bound to its role:
- Native audio controls and embedded-video-audio behavior:
- Voice-reference support actually exposed:
- Start / end frame support actually exposed:
- Duration, resolution and aspect ratios:
- Upload and result-download method:
- Job status / recovery / cancellation method:
- Price quote and which inputs/durations are billed:
- Account/region or transport limitation:
- Verified read-only checks:
- Still uncertain:

A listed product feature is not the same as an exposed MCP input. No paid test is implied by this check.
